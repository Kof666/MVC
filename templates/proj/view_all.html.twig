{% extends 'proj_base.html.twig' %}

{% block title %}Players{% endblock %}

{% block main %}

<div class="blackjack">
    <h2>Saved players</h2>
    {# {{ dump(hands) }} #}
    {# {{ dump(game_info) }} #}
    {{ include('card/flash.html.twig') }}
    <table>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Account</th>
            <th>Number of hands</th>
            <th>Load</th>
            <th>Delete</th>
            <th>view hands</th>
        </tr>
    {% for players in blackjack %}
        <tr>
            <td>{{ players.getId|e }}</td>
            <td>{{ players.getName|e }}</td>
            <td>{{ players.getAccount|e }}</td>
            <td>{{ players.getNumOfHands|e }}</td>
            <td>
                <a href="{{ path('load', {'id': players.getId})}}"><i class="fa-regular fa-floppy-disk"></i></a>
            </td>
            <td>
                <a href="{{ path('player_delete_by_id', {'id': players.getId})}}"><i class="fa-solid fa-trash"></i></a>
            </td>
            <td>
                <a href="{{ path('hand_by_id', {'id': players.getId})}}"><i class="fa-solid fa-circle-info"></i></a>
            </td>
        </tr>
    {% endfor %}
    </table>
    
    <button class="button" role="button">
        <a href="{{ path('proj')}}">back</a>
    </button>

    <button class="button" role="button">
        <a href="{{ path('reset')}}">Reset database</a>
    </button>
</div>

{% endblock %}
