<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Blackjack!{% endblock %}</title>
        <script src="https://kit.fontawesome.com/4c8b6b3b50.js" crossorigin="anonymous"></script>
        <link rel="icon" href="{{ asset('img/dice2.jpg') }}">
        {% block stylesheets %}
            {{ encore_entry_link_tags('proj') }}
        {% endblock %}

        {# {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %} #}
    </head>
    <body>
        <header>
            <nav>
                <ul>
                    <li><a href="{{ path('proj') }}">Start</a></li>
                    <li><a href="{{ path('about_proj') }}">about</a></li>
                    <li><a href="{{ path('me') }}">quit</a></li>
                </ul>
            </nav>
            {% block header %}{% endblock %}
        </header>
        <main>
            {% block main %}{% endblock %}
        </main>        
        <footer>
            <p>Denna sidan är Copyright &copy; av Kristoffer Berg.</p>
            <a href="{{ path('session_destroyed') }}">destroy session</a>
        </footer>
    </body>
</html>
